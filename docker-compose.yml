services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/sqlite.db
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
